# Public
NEXT_PUBLIC_API_URL=http://localhost:3000/v1
# After `npm run db:seed` this matches the default demo company (unless SEED_COMPANY_ID overrides)
NEXT_PUBLIC_DEFAULT_COMPANY_ID=00000000-0000-4000-8000-000000000001
# Optional: VAT rate 0–1 for desk rate-quote display only (0 hides VAT lines). Default 0.22 in code.
# NEXT_PUBLIC_VAT_RATE=0.22
