# Main features — implementation queue (core first)

Order for **in-repo** work. Skip rows marked *addon* unless they unblock you. See [PRODUCTION-READINESS.md](PRODUCTION-READINESS.md) for the full catalog (including ops/legal).

| # | Item | Status |
|---|------|--------|
| 1 | **G1 — Fleet & class utilization** in company report (half-open overlap vs UTC range) | Done |
| 2 | **H1** — OpenAPI: document remaining `v1` routes (`@ApiOperation`, `@ApiBearerAuth`, `@ApiTags`) | Done |
| 3 | **A2 (code)** — Stricter password rules on register + change + reset; optional account lockout after N failures | Done |
| 4 | **C1 (core)** — Clear public quote status / pay CTA (no multi-vehicle basket) | Done |
| 5 | **E1** — Tighter Stripe ↔ booking reconciliation *views* in desk (export already exists) | Done |
| 6 | **C3 (core)** — Guest booking view polish (copy link, deposit wording, next steps, quote `?companyId=`) | Done |
| 7 | **B1 (desk)** — Reservation list filter `?customerId=` + deep link from **Customers** | Done |
| 8 | **H2 (desk)** — Dedicated **Account** page (password + TFA copy); **Nav** link; shared **DeskChangePasswordCard** | Done |
| 9 | **C2** — Rental **paid** webhook email includes magic **View booking** link (when token + `APP_PUBLIC_BASE_URL`) | Done |
| 10 | **C2** — **Booking confirmed** email when desk **PATCH**es reservation to `CONFIRMED` (SMTP; magic link when token + base URL) | Done |
| 11 | **D1–D2 (desk)** — Reservation **list** + **CSV**: agreement template version + e-sign audit fields (tooltip) | Done |
| 12 | **H3** — Cookie **EN/IT**: public shell + **desk** nav/dashboard/**account** + **`/desk/audit`**, **`/desk/reports`**, **`/desk/reconciliation`**, **`/desk/invoices`**, **`/desk/organization`**, **`/desk/customers`**, **`/desk/fleet`**, **`/desk/reservations`** (list / filters / **ReservationStatusActions** / **`ReservationForm`** / **`ReservationOpsPanel`**), **`/desk/team`**, **`CompanyScopeSelect`** | Done |
| 13 | *H3 polish (optional):* translate remaining **API**-originating UI strings (e.g. ad-hoc validation messages); audit edge hardcoded English | **Partial** — **`apiJson`** / downloads translate errors; **Zod** via **`translateDeskApiError`** (desk + **register**); **`useMe` / `useCompanyScope`**, **calendar**; **`/desk/invoices`** inline action errors + **SDI status** **`desk.invoices.sdi.status.*`**; SDI **FAILED** tooltip + **`ReservationForm`** CaRGOS lines use **`translateDeskApiErrorLine`** on stored **`errorMessage`**; **`ReservationForm`** refund **`desk-ok`**; **`/desk/audit`** damage **DRAFT** / **CLOSED** + **`customer.merge`** detail (**EN/IT**); CaRGOS **`desk.reports.enum.cargos.*`**; **`CustomerForm`** recent reservations (**status** + **source**) + duplicate hints (**email**, **phone**, fiscal, VAT); **`/desk/reservations`** table CaRGOS column (localized + error tooltip); **`/desk/reconciliation`** (**reservation status**, **`desk.reconciliation.depositHold.*`**, **PARTNER** source); shared **`desk-deposit-hold-label`** (**reservations** list + **ReservationForm** deposit line); **`desk-rental-agreement-status`** (**reservations** agreement column + **ReservationForm** agreement block); **`desk.ui.buttonBusy`** (in-flight **desk** buttons: reservations CaRGOS / quick actions, **ReservationForm** Stripe actions, **Partner API** keys); **B1 desk:** reservations **CSV `customerId`**, **profile** link column, **`?customerId=`** banner (**name/email**) |
| 14 | **B3 (company)** — Fiscal fields on **Company** (lessor): **`PATCH /companies`** + desk **Organization** | Done |
| 15 | **E4 (HTTP payload)** — SDI middleware POST includes **supplier** + **buyer** fiscal snapshot (B3) | Done |
| 16 | **E3 (desk + API)** — Invoice **detail** exposes lessor + buyer B3; **fiscal snapshot** on **`/desk/invoices`** | Done |
| 17 | **E3 (desk)** — New draft: **use booking total** fills subtotal (**ex. VAT**) from **`GET /reservations/:id`** | Done |
| 18 | **E3 (desk)** — **Edit draft**: optional **reservation** link / clear (`PATCH` `reservationId`); **use booking total** for **INVOICE** drafts | Done |
| 19 | **B2 (worker)** — Purge **`CustomerDocument`** past **`retentionUntil`** (blob + DB); **`WORKER_RETENTION_PURGE_BATCH`** | Done |
| 20 | **A3 (codes)** — MFA TOTP **backup / recovery codes** (`User.mfaBackupCodeHashes`, login + desk regen) | Done |
| 21 | **G1 (H3)** — Report **utilization** formula: API **`utilization.definitionKey`** + **`desk.reports.utilization.definition`** (**EN/IT**) | Done |
| 22 | **A6 (throttler)** — Optional **`REDIS_URL`**: **`RedisThrottlerStorage`** for **`@nestjs/throttler`** (shared limits across API replicas); in-memory when unset | Done |
| 22b | **A6 (metrics)** — **`GET /v1/metrics`** Prometheus exposition (`prom-client` defaults + HTTP counters + integration queue gauges); health probes unchanged | Done |
| 23 | **D4 (CaRGOS)** — HTTP adapter **POST** body **`specVersion` 1** (`CargosHttpAdapterPayload`): trip + vehicle/class + station + customer + agreement; enqueue **idempotent** for **PENDING**/**PROCESSING** | Done |
| 24 | **D4 / D3** — **Auto-enqueue CaRGOS** when rental agreement **SIGNED** (same as **`POST /integrations/cargos/enqueue`**); failures → **`rental_agreement.cargos_auto_enqueue_failed`** audit; **`CARGOS_AUTO_ENQUEUE_ON_SIGN`** (**0**/off to disable) | Done |
| 25 | **G2 (partial)** — **`PartnerApiKey`**, **`Reservation.createdByPartnerApiKeyId`** (partner create; staff/Stripe **`status`** → **`reservation.status_changed`** webhook; SQL backfill **`20260519120000_backfill_reservation_created_by_partner_key_g2`** for legacy **`PARTNER`** rows with idempotency), **`GET /v1/partner/me`** (integration context), **`POST /v1/partner/reservations`**, **`PATCH /v1/partner/reservations/:id`** (cancel-only **`{ "status": "CANCELLED" }`**, **`reservation.cancelled`** webhook), **`GET /v1/partner/reservations`** (paged **PARTNER** list, optional **`status`**) + **`GET …/:id`** (Bearer = legacy key **or** OAuth2 access JWT from **`POST /v1/partner/oauth/token`**; optional **`PARTNER_MTLS_REQUIRE`** + edge **client-cert verified** header), desk **Organization** partner keys + **OAuth `client_secret`** (`POST …/partner-api-keys/:id/oauth-client-secret`) + per-key **HTTPS webhook** + **`PATCH …/partner-api-keys/:id/webhook`** + per-key **IPv4/CIDR allowlist** + **`PATCH …/partner-api-keys/:id/allowed-ip-cidrs`** (AND with env **`PARTNER_API_ALLOWED_IP_CIDRS`** when both set), desk **webhook delivery log** + **`GET /companies/:id/partner-webhook-deliveries`** ( **`READONLY_ACCOUNTING`**: read-only keys + log), **`PartnerWebhookDelivery`** queue (**`reservation.created`** persisted body + **HMAC** **`POST`** via **`POST …/internal/cron/partner-webhook-deliveries`**, backoff, **`DEAD`**), **`GET /health/summary`** **`partnerWebhookPending`**, worker **`WORKER_PARTNER_WEBHOOK_INTERVAL_MS`**, **`ReservationSource.PARTNER`**; **`Idempotency-Key`** + **`PartnerReservationIdempotency`**; **`@Throttle`** | Partial ([PRODUCTION.md](PRODUCTION.md#partner-api-and-document-ocr-g2-g3)) |
| 26 | **G3 (partial)** — Mock / **HTTP** OCR + staff apply/dismiss; **`CUSTOMER_DOCUMENT_OCR_AUTO=mock|http`** (http → **`CUSTOMER_DOCUMENT_OCR_HTTP_URL`** + optional secret/timeout/vendor; presign GET + HMAC request headers when configured); **`POST /v1/internal/cron/customer-document-ocr`** + async **`POST …/customer-document-ocr-callback`** (**`documentId` + `suggestion` _or_ `error`**); **`GET /health/summary`** `customerDocumentOcrPending`, worker **`WORKER_CUSTOMER_DOCUMENT_OCR_INTERVAL_MS`** | Partial (same §) |
| 27 | **A3 (enrollment lock)** — **`mfa_setup`** JWT + **`JwtAuthGuard`** allowlist (`/auth/me`, MFA setup confirm/cancel); **`JwtStrategy`** rejects full JWT when **`AUTH_MFA_REQUIRED`** and privileged user has no MFA; **`confirmMfaSetup`** returns full **`accessToken`**; web **`/auth`** enrollment step | Done |

*Still later / not full product:* production-grade **G2** (published SLA, **partner-facing** self-service observability beyond the **desk + `GET …/partner-webhook-deliveries`** log; IdP-grade OAuth / in-process mTLS beyond the repo **client_credentials + edge mTLS hook**), **G3** real vendor integration + **DPA**/region beyond HTTP adapter hook, **E5** extra PSP; **H3** remaining API strings — see [PRODUCTION-READINESS.md](PRODUCTION-READINESS.md) §G / §H. **Deploy / runbooks** — [PRODUCTION.md](PRODUCTION.md), [STAGING.md](STAGING.md).

Update this file when a row ships.
